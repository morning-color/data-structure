#include <iostream>
#include "linked_list.h"
#include "common.h"
#include <cstdio>

template <typename T>
void print_data(T data)
{
	std::cout  << data << std::endl;
}

int main(int argc, char const *argv[])
{
	// linked_list<char> l;
	// print_it(l.length());
	// print_it(l.empty());
	// l.modify_elem(1,'x');
	// l.insert_elem(1,'y');
	// l.insert_after('a');
	// print_it(l.empty());
	// l.insert_after('b');
	// l.insert_after('c');
	// l.insert_after('d');
	// l.insert_after('e');
	// l.insert_elem(3,'z');
	// l.reset();
	// l.get_elem(7);
	// l.del_next();
	// l.traverse(print_data);
	// l.del_elem('d');
	// puts("FFFFFFFFFFFFFFf");
	// l.traverse(print_data);
	// auto pos = l.locate('x');
	// print_it(pos);
	// print_it(l.length());
	// print_it(l.prior()->get_data());
	// print_it(l.behind()->get_data());
	// print_it(l.get_elem(1)->get_data());
	// print_it(l.get_elem(2)->get_data());
	// print_it(l.get_elem(3)->get_data());
	// print_it(l.get_elem(7)->get_data());


	fflush(stdout);
	exit(0);
	return 0;
}