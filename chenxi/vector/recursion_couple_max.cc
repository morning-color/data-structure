#include <iostream>

template<typename T, typename Arg>
find_two_max_elem(const T&& arr, Arg& max1, Arg& max2)
{
	int len = sizeof(arr) / sizeof(arr[0]);
	BOOST_ASSERT_MSG(len > 0,"输入数组长度")
}

int main()
{
	return 0;
}